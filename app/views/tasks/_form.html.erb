<%= form_with(model: task, local: true) do |f| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
    <%= f.label :project_id %><br />
    <%= f.select :project_id, options_for_select(Project.all.pluck(:p_name,:id), :selected => f.object.project_id), {}, {}%>
  </div>
  <div class="field">
    <%= f.label :t_name, "Task Name" %><br />
    <%= f.text_field :t_name %>
  </div>
  <div class="field">
    <%= f.label :t_discription, "Task Description" %><br />
    <%= f.text_field :t_discription %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
